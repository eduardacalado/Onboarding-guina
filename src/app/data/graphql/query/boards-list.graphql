query Boards($pageInput: PageInput!) {
  boards(pageInput: $pageInput) {
    nodes {
      id
      name
    }
    count
    pageInfo {
      offset
      limit
      hasNextPage
      hasPreviousPage
    }
  }
}
